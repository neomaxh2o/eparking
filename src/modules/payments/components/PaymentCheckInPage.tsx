import PaymentCheckIn from '@/app/components/Payments/PaymentCheckIn';

// Compatibility UI entrypoint. Canonical session lifecycle ownership lives in parking-sessions.
export default function PaymentCheckInPageModule() {
  return <PaymentCheckIn />;
}
